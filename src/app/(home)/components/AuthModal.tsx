"use client"
import { Modal } from "@/components"

export const AuthModal = () => {
    return (
        <Modal
            title="Auth"
            description="Sign in for the best experience"
            isOpen
            onChange={() => {}}
        >

        </Modal>
    )
}